const is42TheAnswertToEverything = Math.random() + Math.random();

if (is42TheAnswertToEverything === 42) {
  console.log(`At least here is was true`)
} else {
  console.log(`hmmm... 42 shouldn't be the answer to everything? here it wasn't`);
}
